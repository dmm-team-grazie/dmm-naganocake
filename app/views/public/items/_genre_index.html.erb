<h2>ジャンル一覧</h2>
<div id="list-example" class="list-group">
  <% genres.each do |genre| %>
    <h4 class="list-group-item list-group-item-action"> <%= link_to genre.name, public_genre_path(genre) %></h4>
  <% end %>
</div>
