<div class="row">
  <div class="col-xs-3">
    ジャンル一覧
  </div>

  <div class="col-xs-9">
    <h2>商品一覧  (全<%= @items.count %>件)</h2>
    <div class="row row-cols-1 row-cols-md-3">
      <% @items.each do |item| %>
        <div class="col mb-3">
          <div class="card">
            <%= attachment_image_tag item, :item_image, :fill, 160, 90, fallback: "no-image.jpg", class: "card-img-top" %>
            <div class="card-body">
              <h5 class="card-title"><%= link_to item.name, item_path %></h5>
              <p class="card-text">¥<%= item.non_taxed_price %></p>
            </div>
          </div>
        </div>
      <% end %>
      <%= paginate @items %>
    </div>
  </div>
</div>
